import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("Name")
                .frame(width: 100, height: 100, alignment: .trailing)
            
            Text("Last Name")
                .frame(width: 100, height: 100, alignment: .trailing)
            
           }
        }
    }

